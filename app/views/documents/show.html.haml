.wrapper_with_padding
  = link_to 'Back to all documents', documents_path, class: 'back-link  u-m0'
  #document_show
    %h1.title= @document.title
    %p.document_date= @document.created_at.strftime("%d %B %Y - %H:%M")
    %p= simple_format @document.content

    .document_actions
      = link_to 'All documents', documents_path, class: 'button button--small'
      = link_to 'Edit document', edit_document_path(@document), class: 'button button--small'
      = link_to 'Delete document', document_path(@document), method: :delete, data: { confirm: 'Are you sure?' }, class: 'button button--small'
